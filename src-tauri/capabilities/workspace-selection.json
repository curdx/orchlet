{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace-selection",
  "description": "Capability for the workspace selection window",
  "windows": ["workspace-selection"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-path"
  ]
}
