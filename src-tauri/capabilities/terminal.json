{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "terminal",
  "description": "Capability for the terminal window placeholder",
  "windows": ["terminal"],
  "permissions": [
    "core:default"
  ]
}
